\section{Hierarchical Sea-Surface Modeling}







The proposed framework maps environmental conditions and random realizations to radar-ready dynamic sea surfaces. The environmental input is $\boldsymbol{p}_e=\{U_{10},\phi_w,\Omega_c,H_s,L_x,L_y,\Delta x,\Delta y\}$, where $U_{10}$, $\phi_w$, $\Omega_c$, and $H_s$ specify the sea state, while the remaining quantities define the computational domain. A random variable set $\boldsymbol{\xi}$ controls the Fourier phases, allowing independent samples to be generated under identical environmental conditions. Radar parameters are introduced only after the surface has been triangulated, so the sea state and observation configuration can be varied independently.

\begin{figure*}[!t]
\centering
\fbox{\parbox[c][3.5cm][c]{0.94\textwidth}{\centering \textbf{Placeholder for the overall framework figure}\\[4pt] Environmental parameters and random phases $\rightarrow$ Elfouhaily spectral sampling and nonlinear Lie transformation $\rightarrow$ equal-energy directional short-wave replacement $\rightarrow$ steep-crest detection and localized curling $\rightarrow$ triangular facets and radar echoes.}}
\caption{Overall framework of the proposed sea-surface and radar-echo simulation method.}
\label{fig:overall_framework}
\end{figure*}

The complete generation chain is summarized as
\begin{equation}
\begin{aligned}
(\boldsymbol{p}_e,\boldsymbol{\xi})
&\xrightarrow{\;\mathcal{G}_{NL}\;}\eta_N
\xrightarrow{\;\mathcal{D}_s\;}\eta_D\\
&\xrightarrow{\;\mathcal{C}\;}\mathcal{M}_C
\xrightarrow{\;\mathcal{R}(\boldsymbol{p}_r)\;}\boldsymbol{y},
\end{aligned}
\label{eq:complete_simulation_chain}
\end{equation}
where $\mathcal{G}_{NL}$ denotes nonlinear spectral background generation, $\mathcal{D}_s$ denotes equal-energy directional short-wave replacement, $\mathcal{C}$ denotes localized curling and triangulation, and $\mathcal{R}$ denotes facet-based radar simulation. The first two operators act over the full domain using Fourier transforms, whereas the curling operator is restricted to selected steep crests. This separation retains large-area statistical control while limiting the additional geometric computation to radar-relevant local structures.












\subsection{Nonlinear Background and Directional Wind Waves}
\label{subsec:nonlinear_directional_surface}

The first modeling stage combines a nonlinear broadband background with an equal-energy directional reorganization of its resolved short-wave band. The background supplies sea-state-dependent spectral statistics and non-Gaussian crest geometry, while the reorganized band produces short wind-wave ridges with a controllable dominant propagation direction without duplicating energy already present in the Elfouhaily spectrum.

Let $k=(k_x^2+k_y^2)^{1/2}$ and $\varphi=\operatorname{atan2}(k_y,k_x)$. The directional elevation spectrum is written as~\cite{elfouhaily1997unified}
\begin{equation}
\Psi_{\eta}(k,\varphi)=\frac{B_l(k)+B_h(k)}{k^4}D(k,\varphi-\varphi_w),
\label{eq:elfouhaily_2d}
\end{equation}
where $B_l$ and $B_h$ are the gravity- and capillary-wave curvature spectra and $D$ is the Elfouhaily spreading function. On the discrete wavenumber grid, the linear coefficients are sampled as
\begin{equation}
\widehat{\eta}_L(\boldsymbol{k})=\left[\Psi_{\eta}(\boldsymbol{k})\Delta k_x\Delta k_y\right]^{1/2}\xi(\boldsymbol{k}),
\label{eq:linear_spectral_sampling}
\end{equation}
where $\xi$ is a unit-variance complex Gaussian field. Hermitian symmetry is imposed explicitly, and the same random phases are retained during the nonlinear transformation.

To express the Lie correction relative to the wind, the along- and cross-wind wavenumbers are defined as $k_a=k_x\cos\varphi_w+k_y\sin\varphi_w$ and $k_c=-k_x\sin\varphi_w+k_y\cos\varphi_w$. The corresponding Riesz fields are
\begin{equation}
\widehat{h}_a=-\mathrm{i}\frac{k_a}{k}\widehat{\eta}_L\mathcal{B}_i,
\qquad
\widehat{h}_c=-\mathrm{i}\frac{k_c}{k}\widehat{\eta}_L\mathcal{B}_i,
\label{eq:riesz_fields}
\end{equation}
where $\mathcal{B}_i$ is the input-band mask. Following the second-order Lie/Creamer construction~\cite{zhang2023secondorder}, the nonlinear correction is
\begin{align}
\widehat{L}={}&-\frac{k_a^2}{2k}\mathcal{F}\{h_a^2\}
-\frac{k_ak_c}{k}\mathcal{F}\{h_ah_c\}
\nonumber\\
&-\frac{k_c^2}{2k}\mathcal{F}\{h_c^2\},
\label{eq:second_order_lie}
\end{align}
and the preliminary nonlinear spectrum becomes
\begin{equation}
\widehat{\eta}_{N,0}=\widehat{\eta}_L+\beta\mathcal{P}_H\!\left\{\mathcal{B}_o\widehat{L}\right\},
\label{eq:preliminary_nonlinear_spectrum}
\end{equation}
where $\beta$ is dimensionless, $\mathcal{B}_o$ is the output-band mask, and $\mathcal{P}_H$ denotes Hermitian projection. The Riesz multipliers and $\beta$ are dimensionless; hence, no dimensional wind-speed factor is directly multiplied by the elevation correction. The input and output limits are set to $4k_p$ and $8k_p$, respectively, to retain the resolved quadratic interactions.

Because the nonlinear operation redistributes slope energy, its along- and cross-wind mean-square slopes (MSSs) are closed against the same Elfouhaily spectrum used for synthesis. For $q\in\{a,c\}$,
\begin{align}
m_q&=\sum_{\boldsymbol{k}}k_q^2\left|\widehat{\eta}(\boldsymbol{k})\right|^2,
\nonumber\\
G(\varphi)&=\exp\!\left[\frac{\lambda_0+\lambda_2\cos2(\varphi-\varphi_w)}{2}\right].
\label{eq:mss_closure}
\end{align}
Independent calibration realizations determine smooth wind-dependent along- and cross-wind scale factors by matching the median raw nonlinear MSSs to the integrated Elfouhaily MSSs. For each subsequent realization, $\lambda_0$ and $\lambda_2$ are solved to realize these fixed scale factors without altering Fourier phase. Cox--Munk and the other empirical slope models are therefore reserved for external validation rather than used as fitting constraints. The closed nonlinear background spectrum is $\widehat{\eta}_N=G(\varphi)\widehat{\eta}_{N,0}$.

The broadband background already contains short-wave energy, but it does not ensure an organized local propagation direction. A target wavelength interval $[\lambda_{s,\min},\lambda_{s,\max}]$ is therefore selected, with $k_l=2\pi/\lambda_{s,\max}$ and $k_h=2\pi/\lambda_{s,\min}$. Let $W_s(k)$ be a raised-cosine radial window over this interval. The retained background is
\begin{equation}
\widehat{\eta}_r(\boldsymbol{k})=\left[1-W_s(k)\right]\widehat{\eta}_N(\boldsymbol{k}).
\label{eq:short_wave_band_removal}
\end{equation}
For a prescribed propagation unit vector $\boldsymbol{e}_s=(\cos\psi_s,\sin\psi_s)$, the replacement-band shape is
\begin{equation}
\Phi_s(\boldsymbol{k})=W_s(k)\exp\!\left[-\frac{\log^2(k/k_0)}{2\sigma_k^2}-\frac{\theta_a^2}{2\sigma_\theta^2}\right],
\label{eq:directional_short_wave_shape}
\end{equation}
where $k_0=\sqrt{k_lk_h}$, $\sigma_k=\log(k_h/k_l)/4$, and $\theta_a=\cos^{-1}(|\boldsymbol{k}\cdot\boldsymbol{e}_s|/k)$. The angular term concentrates the band around the prescribed axis and represents the directional organization observed for short wind waves~\cite{sergievskaya2024shortwaves}.

A preliminary replacement spectrum is sampled as $\widehat{\eta}_{s,0}=\mathcal{P}_H\{\sqrt{\Phi_s}\,\xi_s\}$. Its scale is determined from the removed-band energy:
\begin{equation}
\alpha_s=\left[\rho_s\frac{\displaystyle\sum_{\boldsymbol{k}}|W_s\widehat{\eta}_N|^2}{\displaystyle\sum_{\boldsymbol{k}}|\widehat{\eta}_{s,0}|^2}\right]^{1/2}.
\label{eq:short_wave_energy_normalization}
\end{equation}
With $\rho_s=1$, the replacement and removed bands have equal variance by Parseval's theorem. This is a spectral reorganization rather than an additive energy injection.

Unidirectional evolution is introduced by assigning opposite temporal phases to the Hermitian wavenumber pair. The combined surface is
\begin{align}
\eta_D(\boldsymbol{x},t)=\operatorname{Re}\mathcal{F}^{-1}\!\bigg\{&\widehat{\eta}_r(\boldsymbol{k})\mathrm{e}^{-\mathrm{i}\omega(k)t}
\nonumber\\
&+\alpha_s\widehat{\eta}_{s,0}(\boldsymbol{k})\mathrm{e}^{-\mathrm{i}\omega(k)\operatorname{sgn}(\boldsymbol{k}\cdot\boldsymbol{e}_s)t}\bigg\},
\label{eq:combined_nonlinear_directional_surface}
\end{align}
where $\omega(k)=\sqrt{gk}$ for the resolved short gravity waves. Unless otherwise stated, $\lambda_{s,\min}=1.5$~m, $\lambda_{s,\max}=5.0$~m, $\sigma_\theta=8^{\circ}$, and $\rho_s=1$. The minimum wavelength is represented by at least six grid samples. The resulting $\eta_D$ retains the nonlinear broadband background while providing directionally organized short crests for the local curling stage.


\begin{figure}[!t]
    \centering
    \includegraphics[width=\columnwidth]{plotcode/output/Fig_IIA_nonlinear_directional_4panel.pdf}
    \caption{Nonlinear-background and directional short-wave construction at $U_{10}=10~\mathrm{m/s}$, $\Omega_c=0.84$, and $\phi_w=\psi_s=0^\circ$, over a $128\times128~\mathrm{m}^2$ domain with $0.25~\mathrm{m}$ sampling. (a) Linear and nonlinear propagation-direction profiles. (b) Removed and equal-energy replacement short-wave components for $\lambda_s=1.5$--$5.0~\mathrm{m}$, $\sigma_\theta=8^\circ$, and $\rho_s=1$. (c) Combined sea-surface top view. (d) Local 3-D geometry near the selected crest.}
    \label{fig:nonlinear_directional_surface}
\end{figure}







\subsection{Localized Crest Detection and Curling Geometry}
\label{subsec:localized_curl}

The nonlinear directional surface $\eta_D$ remains single valued and therefore cannot represent a forward-overturning crest. A localized parametric transformation is consequently applied only to selected steep crests. The construction is intended to retain radar-relevant upper-surface geometry at substantially lower cost than resolving the complete air--water flow; it does not model entrained air, spray, or the full hydrodynamic evolution of breaking waves. Similar finite breaker geometries have been used to study localized electromagnetic signatures~\cite{holliday1998steepening,li2017facet,dong2023plunging}.

Let $\boldsymbol{e}_s=(\cos\psi_s,\sin\psi_s)$ denote the dominant propagation direction and $\partial_u=\boldsymbol{e}_s\cdot\nabla$. Candidate crests are restricted to the set
\begin{equation}
\mathcal{D}_c=\left\{\boldsymbol{x}:\eta_D\geq h_q,\;\partial_u^2\eta_D<0,\;|\partial_u\eta_D|\leq s_c\right\},
\label{eq:crest_candidate_set}
\end{equation}
where $h_q$ is a prescribed elevation quantile and $s_c$ is a near-crest slope tolerance. Edge points that cannot contain the complete curling support are excluded. Within $\mathcal{D}_c$, the event center is selected using
\begin{equation}
\boldsymbol{x}_c=\underset{\boldsymbol{x}\in\mathcal{D}_c}{\arg\max}\;Q(\boldsymbol{x}),
\qquad
Q=w_h\widetilde{\eta}
+w_\kappa\widetilde{(-\partial_u^2\eta_D)}
+w_s\widetilde{\eta_s},
\label{eq:crest_selection_score}
\end{equation}
where tildes denote normalized positive quantities and $\eta_s$ is the directional short-wave component. This rule favors high crests with negative directional curvature and a locally strong organized short-wave contribution. It is used to place a parameterized breaking event and is not presented as a universal hydrodynamic breaking-onset criterion. The individual detection gates and their joint selection are illustrated in Fig.~\ref{fig:curl_detection_conditions}.

\begin{figure}[!t]
    \centering
    \includegraphics[width=\columnwidth]
    {plotcode/output/01_curl_detection_conditions.pdf}
    \caption{Breaking-eligible crest selection for $U_{10}=5~\mathrm{m/s}$,
    $\Omega_c=0.84$, and $H_s=0.35~\mathrm{m}$ over a
    $32\times32~\mathrm{m}^2$ surface. The detection uses an elevation
    quantile of $0.88$, a slope-tolerance factor of $0.32$, and a
    negative-curvature quantile of $0.70$. (a)--(c) Elevation, near-crest
    slope, and curvature conditions. (d) Joint candidates, selected crest,
    and local curling support.}
    \label{fig:curl_detection_conditions}
\end{figure}

At $\boldsymbol{x}_c=(x_c,y_c)$, propagation and crestwise coordinates are defined as
\begin{equation}
\begin{bmatrix}u\\v\end{bmatrix}=
\begin{bmatrix}
\cos\psi_s&\sin\psi_s\\
-\sin\psi_s&\cos\psi_s
\end{bmatrix}
\begin{bmatrix}x-x_c\\y-y_c\end{bmatrix}.
\label{eq:curl_local_coordinates}
\end{equation}
Let $a=L_c/2$ and $b=W_c/2$ be the crestwise half-length and propagation-direction half-width. A compact cosine window is defined by
\begin{equation}
C(q)=
\begin{cases}
\frac{1}{2}[1+\cos(\pi q)],&|q|<1,\\
0,&|q|\geq1.
\end{cases}
\label{eq:compact_cosine_window}
\end{equation}
To avoid a perfectly regular footprint, a small centerline displacement $u_c(v)$ and width factor $r(v)$ are introduced and $u_r=u-u_c(v)$ is used below. The core and transition supports are
\begin{align}
W_c&=C\!\left(\frac{u_r}{br(v)}\right)
C\!\left(\frac{v}{a}\right),
\nonumber\\
W_t&=C\!\left(\frac{u_r}{(b+\ell_t)r(v)}\right)
C\!\left(\frac{v}{a}\right),
\label{eq:curl_support_windows}
\end{align}
where $\ell_t>0$ extends the transition region beyond the rotation core. Both windows vanish continuously at their boundaries, while $W_t$ permits the neighboring non-overturned surface to evolve smoothly before returning to the unchanged background.

The approaching crest is first modified without rotation. An asymmetric width
\begin{equation}
\sigma(u_r)=
\begin{cases}
\sigma_r,&u_r<0,\\
\sigma_f,&u_r\geq0,
\end{cases}
\qquad \sigma_f<\sigma_r,
\label{eq:asymmetric_crest_width}
\end{equation}
produces a broader rear face and a steeper forward face. With $\sigma_e$ denoting the transition scale, the pre-rotation coordinates are
\begin{align}
z_p={}&\eta_D
+A_c\exp\!\left[-\frac{u_r^2}{2\sigma^2(u_r)}\right]W_t
+A_e\exp\!\left[-\frac{u_r^2}{2\sigma_e^2}\right]W_t,
\nonumber\\
u_p={}&u
+A_l\exp\!\left[-\frac{u_r^2}{2\sigma_e^2}\right]W_t.
\label{eq:pre_rotation_evolution}
\end{align}
Here, $A_c$ controls the local crest correction, whereas $A_e$ and $A_l$ produce the low-amplitude elevation and forward-lean changes across the wider evolution band. This separation prevents the curl from appearing as an isolated bump placed directly at the highest point.

The rotation center is shifted by $d_c<0$ toward the approaching shoulder, and the local angle field is
\begin{equation}
\theta(u,v)=\theta_{\max}
\exp\!\left[-\frac{(u_r-d_c)^2}{2(0.62b)^2}\right]
C\!\left(\frac{v}{a}\right).
\label{eq:curl_angle_field}
\end{equation}
The rotation axis is parallel to the crest direction, with pivot coordinates $u_0=u_c(v)+d_c+d_f$ and $z_0=\eta_D-d_p$, where $d_f$ is the forward offset and $d_p$ is the pivot depth. Defining $\Delta u=u_p-u_0$ and $\Delta z=z_p-z_0$, the rotated coordinates are
\begin{equation}
\begin{bmatrix}u_R\\z_R\end{bmatrix}=
\begin{bmatrix}u_0\\z_0\end{bmatrix}+
\begin{bmatrix}
\cos\theta&\sin\theta\\
-\sin\theta&\cos\theta
\end{bmatrix}
\begin{bmatrix}\Delta u\\\Delta z\end{bmatrix}.
\label{eq:local_curl_rotation}
\end{equation}
The final local coordinates are blended with the wider transition support:
\begin{align}
u'&=u_p+W_t(u_R-u_p),
&
z'&=z_p+W_t(z_R-z_p),
\nonumber\\
v'&=v.
\label{eq:curl_transition_blending}
\end{align}
They are mapped back to the global frame by
$x'=x_c+u'\cos\psi_s-v'\sin\psi_s$ and
$y'=y_c+u'\sin\psi_s+v'\cos\psi_s$.
Because the output is the parametric surface
$\boldsymbol{r}_C(u,v)=[x',y',z']^{\mathrm{T}}$
rather than a height function $z=\eta(x,y)$, the geometry can represent locally overlapping horizontal projections. The resulting global embedding and local overturning geometry are shown in Fig.~\ref{fig:curl_global_local_3d}.

\begin{figure}[!t]
    \centering
    \includegraphics[width=\columnwidth]
    {plotcode/output/02_curl_global_local_3d.pdf}
    \caption{Global and local views of the generated curling wave for
    $U_{10}=5~\mathrm{m/s}$, $\Omega_c=0.84$, and
    $H_s=0.35~\mathrm{m}$. The curling patch uses a crestwise half-length
    of $2.20~\mathrm{m}$, a core half-width of $0.42~\mathrm{m}$, and a
    $0.58~\mathrm{m}$ transition width. (a) Curling event embedded in the
    $32\times32~\mathrm{m}^2$ sea surface. (b) Local three-dimensional
    forward-overturning geometry.}
    \label{fig:curl_global_local_3d}
\end{figure}

The propagation-direction section in Fig.~\ref{fig:curl_center_profile} further illustrates the transition from the original crest through the pre-shaped state to the final multi-valued curling profile. In particular, the forward branch advances beyond the original crest before turning downward, while the wider transition support preserves continuity with the surrounding sea surface.

\begin{figure}[!t]
    \centering
    \includegraphics[width=0.5\columnwidth]
    {plotcode/output/03_curl_center_profile.pdf}
    \caption{Propagation-direction section of the selected curling event
    for $U_{10}=5~\mathrm{m/s}$ and $H_s=0.35~\mathrm{m}$, showing the
    original, pre-shaped, and final curled crest profiles.}
    \label{fig:curl_center_profile}
\end{figure}

The core event mask and the actual overturning mask are distinguished as
\begin{align}
M_c&=\mathbf{1}(W_c\geq\tau_c),
&
M_o&=M_c\,\mathbf{1}(J_u<0),
\nonumber\\
J_u&=\boldsymbol{e}_s\cdot\nabla u'.
\label{eq:curl_core_overturning_masks}
\end{align}
$M_c$ identifies facets materially affected by the curling core, whereas $J_u<0$ identifies a reversal of the propagation-coordinate mapping and therefore provides a direct geometric indication of overturning. The transition support $W_t$ is not counted as the breaking core.

Finally, the original structured-grid connectivity is retained to triangulate both the uncurled and curled surfaces. For each face $f=(i,j,k)$, its area and unit normal are
\begin{align}
\boldsymbol{e}_{ij}&=\boldsymbol{r}_j-\boldsymbol{r}_i,
&
\boldsymbol{e}_{ik}&=\boldsymbol{r}_k-\boldsymbol{r}_i,
\nonumber\\
A_f&=\frac{1}{2}
\left\|\boldsymbol{e}_{ij}\times\boldsymbol{e}_{ik}\right\|,
&
\boldsymbol{n}_f&=
\frac{\boldsymbol{e}_{ij}\times\boldsymbol{e}_{ik}}{2A_f}.
\label{eq:curl_facet_geometry}
\end{align}
Preserving the connectivity is essential because retriangulation in the $(x',y')$ projection can incorrectly connect the upper and lower portions of an overturned crest. The paired baseline and curled meshes therefore have identical face indices, allowing changes in local area, orientation, visibility, and radar scattering to be attributed to the curling transformation.










\subsection{Facet-Based Scattering and Radar Echo Generation}
\label{subsec:radar_echo_generation}

The curled sea surface is converted into radar data using a facet-based conditional-mean scattering model followed by resolution-cell statistical echo synthesis. The electromagnetic component is not intended as a new full-wave or two-scale scattering theory. Instead, the resolved sea-surface geometry determines the local facet orientation, projected area, slant range, antenna weighting, and visibility, whereas the unresolved X-band roughness within each medium-scale facet is represented statistically through the Technology Service Corporation (TSC) empirical sea-reflectivity kernel. This scale separation is illustrated schematically in Fig.~\ref{fig:facet_scale_representation}, where the medium-scale sea geometry is explicitly resolved while the embedded short-wave structure is not discretized into individual electromagnetic scatterers. The distinction is also important terminologically: here, TSC denotes the empirical model developed by Technology Service Corporation rather than an abbreviation of ``two-scale composite'' scattering~\cite{karimian2011refractivity}.

\begin{figure}[!t]
    \centering
    \includegraphics[width=0.5\columnwidth]{plotcode/facet_scale_schematic.png}
    \caption{Scale separation between the resolved medium-scale sea facet and the unresolved short-wave roughness represented statistically.}
    \label{fig:facet_scale_representation}
\end{figure}

For facet $m$, let $\boldsymbol{r}_m$, $\boldsymbol{n}_m$, and $A_m$ denote its center position, unit normal, and surface area, respectively, and let $\boldsymbol{r}_R$ denote the radar position. The slant range and unit look direction from the facet toward the radar are
\begin{equation}
R_m=\|\boldsymbol{r}_R-\boldsymbol{r}_m\|,
\qquad
\boldsymbol{s}_m=
\frac{\boldsymbol{r}_R-\boldsymbol{r}_m}{R_m}.
\label{eq:facet_radar_geometry}
\end{equation}
The corresponding local grazing angle and relative wind azimuth are
\begin{equation}
\phi_m=
\sin^{-1}
\left(
\boldsymbol{n}_m\cdot\boldsymbol{s}_m
\right),
\qquad
\vartheta_m=
\operatorname{wrap}_{[-\pi,\pi]}
\left(
\varphi_w-\varphi_m
\right),
\label{eq:facet_grazing_wind_angles}
\end{equation}
where $\varphi_m$ is the radar azimuth viewed from the facet and $\varphi_w$ is the wind direction. Back-facing facets, facets below the prescribed minimum grazing angle, and facets occluded by a larger preceding horizon angle are excluded through a binary visibility factor $V_m$. Consequently, the curling transformation modifies the radar return through both the local facet orientation and geometry-induced self-shadowing.

The conditional mean normalized radar cross section (NRCS) of facet $m$ is evaluated as the sum of empirical diffuse and quasi-specular contributions,
\begin{equation}
\sigma_m^0
=
\sigma_d^0
\left(
\lambda,S,\phi_m,\vartheta_m,U_{10}
\right)
+
\sigma_q^0
\left(
\lambda,S,\phi_m
\right),
\label{eq:tsc_total_nrcs}
\end{equation}
where $S$ denotes the Douglas sea-state index, $U_{10}$ is the $10$-m wind speed, and $\lambda$ is the radar wavelength. For the implemented HH-polarized TSC kernel,
\begin{align}
\sigma_d^0
={}&
\frac{
1.7\times10^{-5}\sqrt{\phi_m}
}{
(3.2808\lambda+0.05)^{1.8}
}
F_aF_UF_\vartheta F_p,
\nonumber\\
\sigma_q^0
={}&
\mu\cot^2\beta
\exp
\left[
-\frac{\cot^2\phi_m}{\tan^2\beta}
\right],
\label{eq:tsc_diffuse_quasi_terms}
\end{align}
with
\begin{align}
\chi
&=
\frac{
4.5416\phi_m
(3.2808\sigma_H+0.25)
}{
\lambda
},
\nonumber\\
F_a
&=
\frac{\chi^{1.5}}{1+\chi^{1.5}},
\qquad
F_U
=
\left(
\frac{1.9438U_{10}+4}{15}
\right)^p,
\nonumber\\
F_p
&=
1-0.6\sin^2\vartheta_m,
\qquad
F_\vartheta
=
\exp(\Gamma_\vartheta),
\nonumber\\
\Gamma_\vartheta
&=
0.3\cos\vartheta_m
\exp(-\phi_m/0.17)
\nonumber\\
&\quad\times
(10.7636\lambda^2+0.005)^{-0.2},
\label{eq:tsc_empirical_factors}
\end{align}
where $\sigma_H=0.03505S^{1.95}$, and $p$, $\mu$, and $\beta$ follow the standard wavelength-, grazing-angle-, and sea-state-dependent TSC parameterization. The TSC output is converted from decibels to linear NRCS before area weighting. This empirical kernel is adopted because it provides a computationally inexpensive conditional mean for a large number of sea-surface facets. Physics-based two-scale scattering models nevertheless remain useful independent references, particularly within the transition region where Bragg and quasi-specular mechanisms coexist~\cite{ye2022improvedTSM}.

The horizontal projected area of facet $m$ is
\begin{equation}
A_{h,m}
=
A_m
\max
\left(
\boldsymbol{n}_m\cdot\boldsymbol{e}_z,0
\right),
\label{eq:facet_projected_area}
\end{equation}
and the corresponding monostatic radar cross-section contribution is
\begin{equation}
\sigma_m
=
V_m\sigma_m^0A_{h,m}.
\label{eq:facet_rcs_contribution}
\end{equation}
Thus, the empirical NRCS represents the unresolved local scattering behavior, whereas the explicitly modeled facet geometry controls the effective scattering area and visibility.

Let $G_m$ denote the one-way antenna power gain associated with facet $m$. The conditional mean received power accumulated in range cell $b$ is
\begin{equation}
P_b
=
\sum_{m\in\mathcal{I}_b}
\frac{
P_t\lambda^2G_m^2
}{
(4\pi)^3R_m^4
}
\sigma_m,
\label{eq:range_cell_mean_power}
\end{equation}
where $\mathcal{I}_b$ denotes the set of visible facets assigned to range cell $b$, and $P_t$ is the transmitted peak power. The summation is therefore carried out at the conditional-power level rather than through coherent evaluation of mutual electromagnetic interactions among all surface facets. The complete computational chain from the resolved sea-surface representation to the synthesized range--time echo is summarized in Fig.~\ref{fig:facet_echo_generation_flowchart}.

\begin{figure}[!t]
    \centering
    \includegraphics[width=\columnwidth]{plotcode/facet_echo_generation_flowchart.png}
    \caption{Processing flow of the facet-based scattering model and statistical radar echo generation.}
    \label{fig:facet_echo_generation_flowchart}
\end{figure}

Slow-time variability is introduced at the resolution-cell level rather than independently for every unresolved subfacet scatterer. A unit-power complex speckle process is constructed from fast and slow components,
\begin{align}
s_{n,b}
&=
\sqrt{1-f_s}\,
s_{n,b}^{(f)}
+
\sqrt{f_s}\,
s_{n,b}^{(s)},
\nonumber\\
s_{n,b}^{(j)}
&=
\rho_j s_{n-1,b}^{(j)}
+
\sqrt{1-\rho_j^2}\,
w_{n,b}^{(j)},
\label{eq:two_time_scale_speckle}
\end{align}
where $w_{n,b}^{(j)}$ denotes a circular complex Gaussian innovation and
\begin{equation}
\rho_j=
\exp
\left[
-\frac{1}{f_{\mathrm{PRF}}\tau_j}
\right].
\end{equation}
A positive correlated texture process $T_{n,b}$ is generated independently and normalized such that $\mathbb{E}[T_{n,b}]=1$. Hence, the texture and speckle components modify the intermittency and temporal persistence of the simulated return without changing the conditional mean power supplied by the facet-based scattering model.

Before pulse compression, the complex baseband return in range cell $b$ is expressed as
\begin{equation}
y_{n,b}^{(0)}
=
\sqrt{P_bT_{n,b}}\,
s_{n,b}
\exp
\left(
-\mathrm{i}\frac{4\pi R_b}{\lambda}
\right)
+
n_{n,b},
\label{eq:resolution_cell_complex_echo}
\end{equation}
where $n_{n,b}$ denotes receiver noise and $R_b$ is the representative slant range of the cell. With the normalized range impulse response $h_q$, the pulse-compressed echo is
\begin{equation}
y_{n,b}
=
\sum_q
h_q
y_{n,b-q}^{(0)}.
\label{eq:pulse_compressed_echo}
\end{equation}
The range--time intensity (RTI) image is then obtained from
\begin{equation}
I_{n,b}=|y_{n,b}|^2,
\end{equation}
and a common reference level is used when displaying and comparing different simulation groups. Echo amplitudes or intensities extracted from a predeclared homogeneous range interval are subsequently used for the distribution, tail-probability, temporal-correlation, and sea-spike analyses described in the validation section.

The resulting implementation should therefore be interpreted as a scalable conditional-mean scattering and statistical echo generator rather than a coherent per-facet, per-pulse full-wave solver. In particular, the TSC empirical kernel does not explicitly represent crest diffraction, foam volume scattering, spray, or multipath interactions between overturning faces. Consequently, differences between paired noncurling and curling cases quantify the radar response induced within the adopted geometry, projected-area, local-angle, visibility, and empirical-reflectivity framework; they should not be interpreted as the complete electromagnetic response of a real breaking wave.
